const pool = require("../db");
const { resolveAuthenticatedFamily } = require("./attachmentsService");
const UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const ok=(payload={})=>({ok:true,payload}); const bad=(status,code,reason)=>({ok:false,status,code,reason}); const isUuid=value=>UUID_RE.test(String(value||"").trim());
async function resolveOwnedAlbum(req,albumId,queryable=pool){const auth=resolveAuthenticatedFamily(req);if(!auth.ok)return auth;if(!isUuid(albumId))return bad(404,"ALBUM_NOT_FOUND","Álbum no encontrado.");const row=(await queryable.query(`SELECT a.id,a.title,a.family_id,a.owner_user_id,a.allow_contributions,a.allow_comments,a.is_archived FROM public.orange_photo_albums a WHERE a.id=$1::uuid AND a.family_id=$2::uuid AND a.owner_user_id=$3::uuid AND a.is_archived=false LIMIT 1`,[albumId,auth.familyId,auth.userId])).rows[0];return row?ok({auth,album:row}):bad(404,"ALBUM_NOT_FOUND","Álbum no encontrado.");}
async function resolveAlbumAccess(req,albumId,queryable=pool){if(!req.user?.id)return bad(401,"NOT_AUTHENTICATED","No autenticado.");if(!isUuid(albumId))return bad(404,"ALBUM_NOT_FOUND","Álbum no encontrado.");const row=(await queryable.query(`SELECT album.id album_id,album.family_id,album.owner_user_id,album.allow_contributions,album.allow_comments,access.subject_type FROM public.orange_photo_albums album LEFT JOIN public.orange_photo_album_access access ON access.album_id=album.id AND access.user_id=$2::uuid AND access.status='active' AND access.revoked_at IS NULL WHERE album.id=$1::uuid AND album.is_archived=false AND (album.owner_user_id=$2::uuid OR access.id IS NOT NULL) LIMIT 1`,[albumId,req.user.id])).rows[0];if(!row)return bad(404,"ALBUM_NOT_FOUND","Álbum no encontrado.");const isOwner=String(row.owner_user_id)===String(req.user.id);return ok({albumId:String(row.album_id),familyId:String(row.family_id),userId:String(req.user.id),isOwner,subjectType:isOwner?"owner":row.subject_type,permissions:{can_view:true,can_contribute:isOwner||row.allow_contributions===true,can_comment:isOwner||row.allow_comments===true}});}
async function listAlbumRecipients(req,albumId,queryable=pool){const owned=await resolveOwnedAlbum(req,albumId,queryable);if(!owned.ok)return owned;const {auth,album}=owned.payload;const [family,external]=await Promise.all([queryable.query(`SELECT auth_user.id user_id,auth_user.email,COALESCE(NULLIF(btrim(person.preferred_name),''),NULLIF(btrim(concat_ws(' ',person.first_name,person.last_name)),''),auth_user.email) display_name,access.status access_status FROM public.family_memberships membership JOIN public.persons person ON person.id=membership.person_id JOIN public.auth_users auth_user ON auth_user.person_id=person.id LEFT JOIN public.orange_photo_album_access access ON access.album_id=$2::uuid AND access.user_id=auth_user.id WHERE membership.family_id=$1::uuid AND membership.status='active' AND auth_user.status IN ('active','pending') AND auth_user.id<>$3::uuid ORDER BY lower(COALESCE(NULLIF(btrim(person.preferred_name),''),person.first_name,auth_user.email))`,[auth.familyId,albumId,auth.userId]),queryable.query(`SELECT DISTINCT ON(auth_user.id) auth_user.id user_id,auth_user.email,auth_user.status auth_status,COALESCE(NULLIF(btrim(person.preferred_name),''),NULLIF(btrim(concat_ws(' ',person.first_name,person.last_name)),''),auth_user.email) display_name,current_access.status access_status FROM public.auth_users auth_user LEFT JOIN public.persons person ON person.id=auth_user.person_id JOIN public.orange_photo_album_access known_access ON known_access.user_id=auth_user.id AND known_access.subject_type='external' JOIN public.orange_photo_albums known_album ON known_album.id=known_access.album_id AND known_album.family_id=$1::uuid LEFT JOIN public.orange_photo_album_access current_access ON current_access.album_id=$2::uuid AND current_access.user_id=auth_user.id WHERE NOT EXISTS(SELECT 1 FROM public.family_memberships membership WHERE membership.person_id=auth_user.person_id AND membership.family_id=$1::uuid AND membership.status='active') ORDER BY auth_user.id,known_access.updated_at DESC`,[auth.familyId,albumId])]);return ok({album:{id:String(album.id),allow_contributions:album.allow_contributions===true,allow_comments:album.allow_comments===true},family:family.rows.map(row=>({key:`family:${row.user_id}`,user_id:String(row.user_id),type:"family",email:row.email,display_name:row.display_name,selected:row.access_status==='active',status:row.access_status||'available'})),external:external.rows.map(row=>({key:`external:${row.user_id}`,user_id:String(row.user_id),type:'external',email:row.email,display_name:row.display_name,selected:row.access_status==='active'||row.access_status==='pending',status:row.access_status||'available'}))});}
module.exports={isUuid,resolveOwnedAlbum,resolveAlbumAccess,listAlbumRecipients};
