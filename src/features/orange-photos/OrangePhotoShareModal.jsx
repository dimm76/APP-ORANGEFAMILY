/* eslint-disable react-hooks/set-state-in-effect */
import {useEffect,useState} from 'react';
export default function OrangePhotoShareModal({photo,members,onClose,onSave}){const [visibility,setVisibility]=useState(photo.visibility),[ids,setIds]=useState([]),[busy,setBusy]=useState(false);useEffect(()=>setVisibility(photo.visibility),[photo]);const toggle=id=>setIds(v=>v.includes(id)?v.filter(x=>x!==id):[...v,id]);return <div className="od-modal-backdrop"><section className="od-modal" role="dialog" aria-modal="true"><header className="od-modal-header"><h2 className="od-modal-title">Compartir foto</h2><button className="od-modal-close" type="button" onClick={onClose}>×</button></header><div className="od-modal-body"><label>Visibilidad<select className="od-filter-input" value={visibility} onChange={e=>setVisibility(e.target.value)}><option value="private">Solo yo</option><option value="family">Toda la familia</option><option value="selected">Miembros concretos</option></select></label>{visibility==='selected'?<fieldset className="od-orange-photo-share__members"><legend>Miembros</legend>{members.map(m=><label key={m.id}><input type="checkbox" checked={ids.includes(m.id)} onChange={()=>toggle(m.id)}/>{m.display_name}</label>)}</fieldset>:null}<div className="od-modal-actions"><button className="od-button-secondary" onClick={onClose}>Cancelar</button><button className="od-modal-primary" disabled={busy||visibility==='selected'&&!ids.length} onClick={async()=>{setBusy(true);await onSave({visibility,user_ids:ids});setBusy(false);onClose();}}>Guardar</button></div></div></section></div>}
