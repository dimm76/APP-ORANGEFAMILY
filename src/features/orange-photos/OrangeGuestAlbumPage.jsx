import { useEffect,useState } from "react"; import OrangeGuestShell from "./OrangeGuestShell.jsx"; import { getOrangeGuestAlbum,listOrangeGuestAlbumPhotos } from "./orangeGuestAlbumApi.js";
export default function OrangeGuestAlbumPage({albumId}){const[state,setState]=useState({loading:true,error:"",album:null,items:[]});useEffect(()=>{Promise.all([getOrangeGuestAlbum(albumId),listOrangeGuestAlbumPhotos(albumId,{page:1,per_page:100})]).then(([a,p])=>setState({loading:false,error:"",album:a.album,items:p.items||[]})).catch(e=>setState({loading:false,error:e.message,album:null,items:[]}));},[albumId]);return <OrangeGuestShell><section className="od-orangephotos-guest-album"><button className="od-button-secondary" type="button" onClick={()=>{window.history.pushState({},"","/guest");window.dispatchEvent(new Event("od-spa-navigate"));}}>Volver</button><h1 className="od-page-title">{state.album?.title||"Álbum"}</h1>{state.error?<p className="od-status-line od-status-line--error">{state.error}</p>:state.loading?<p className="od-status-line">Cargando…</p>:<><p>{state.album?.owner_display_name}</p><div className="od-orangephotos-public__grid">{state.items.map(item=><figure key={item.id}><img src={item.preview_url||item.thumbnail_url} alt={item.title||item.original_filename||"Foto"}/><figcaption>{item.title||item.original_filename}</figcaption></figure>)}</div>{state.album?.permissions?.can_contribute?<button className="od-button-secondary" type="button" disabled>Añadir fotos — disponible en la siguiente fase</button>:null}{state.album?.permissions?.can_comment?<p className="od-inline-msg">Los comentarios estarán disponibles próximamente.</p>:null}</>}</section></OrangeGuestShell>;}
