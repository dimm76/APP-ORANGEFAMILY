import {useState} from 'react';
export default function OrangeAlbumsPanel({albums,active,onPick,onCreate,selectedCount,onAddSelected}){const [title,setTitle]=useState('');return <aside className="od-orange-albums"><h2>Álbumes</h2><button type="button" className={!active?'is-active':''} onClick={()=>onPick('')}>Toda la biblioteca</button>{albums.map(a=><button type="button" className={active===a.id?'is-active':''} key={a.id} onClick={()=>onPick(a.id)}>{a.title}<span>{a.photo_count}</span></button>)}<form onSubmit={e=>{e.preventDefault();if(title.trim()){onCreate(title);setTitle('');}}}><input className="od-filter-input" placeholder="Nuevo álbum" value={title} onChange={e=>setTitle(e.target.value)}/><button className="od-button-secondary">Crear</button></form>{active&&selectedCount?<button className="od-modal-primary" type="button" onClick={onAddSelected}>Añadir {selectedCount} seleccionadas</button>:null}</aside>}
