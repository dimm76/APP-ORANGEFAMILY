import { useState } from "react";
import { createOrangeAlbumPublicLink, revokeOrangeAlbumPublicLink } from "../../shared/api/orangePhotosApi.js";
export default function OrangeAlbumShareModal({album,members,busy=false,error="",onClose,onSave,onPublicLinkChange}){const[visibility,setVisibility]=useState(album.visibility||"private"),[selectedUserIds,setSelectedUserIds]=useState(album.shared_user_ids||[]),[canContribute,setCanContribute]=useState(album.visibility==="family"?album.allow_contributions===true:album.visibility==="selected"?album.shared_can_contribute===true:false),[publicLink,setPublicLink]=useState(album.public_link||{enabled:false,token:null,path:null}),[publicBusy,setPublicBusy]=useState(false),[publicError,setPublicError]=useState(""),[copyFeedback,setCopyFeedback]=useState("");const toggleMember=id=>setSelectedUserIds(current=>current.includes(id)?current.filter(currentId=>currentId!==id):[...current,id]),fullUrl=publicLink.path?`${window.location.origin}${publicLink.path}`:"";const change=async regenerate=>{if(regenerate&&!window.confirm("El enlace anterior dejará de funcionar inmediatamente. ¿Quieres continuar?"))return;setPublicBusy(true);setPublicError("");try{const response=await createOrangeAlbumPublicLink(album.id,regenerate);setPublicLink(response.public_link);onPublicLinkChange?.(response.public_link);}catch(e){setPublicError(e.message);}finally{setPublicBusy(false);}};const revoke=async()=>{if(!window.confirm("Las personas que tengan el enlace dejarán de poder acceder. ¿Quieres continuar?"))return;setPublicBusy(true);setPublicError("");try{const response=await revokeOrangeAlbumPublicLink(album.id);setPublicLink(response.public_link);onPublicLinkChange?.(response.public_link);}catch(e){setPublicError(e.message);}finally{setPublicBusy(false);}};return <div className="od-modal-backdrop"><section className="od-modal" role="dialog" aria-modal="true"><header className="od-modal-header"><h2 className="od-modal-title">Compartir álbum</h2><button className="od-modal-close" type="button" aria-label="Cerrar" onClick={onClose}>×</button></header><div className="od-modal-body od-orange-album-share-modal__body"><div className="od-orange-album-share-modal__section"><label className="od-form-field"><span className="od-form-label">Visibilidad</span><select className="od-filter-input" value={visibility} onChange={event=>{const next=event.target.value;setVisibility(next);if(next==="private")setCanContribute(false);}}><option value="private">Solo yo</option><option value="family">Toda la familia</option><option value="selected">Miembros concretos</option></select></label>{visibility==="selected"?<fieldset className="od-orange-photo-share__members"><legend>Miembros</legend>{members.map(member=><label key={member.id}><input type="checkbox" checked={selectedUserIds.includes(member.id)} onChange={()=>toggleMember(member.id)}/>{member.display_name}</label>)}</fieldset>:null}</div><div className="od-orange-album-share-modal__section"><label className="od-form-field"><span className="od-form-label">Permisos</span><select className="od-filter-input" value={canContribute?"contribute":"view"} disabled={visibility==="private"} onChange={event=>setCanContribute(event.target.value==="contribute")}><option value="view">Solo pueden ver</option><option value="contribute">Pueden añadir fotos y vídeos</option></select></label></div>{error?<p className="od-status-line od-status-line--error">{error}</p>:null}<div className="od-modal-actions"><button className="od-btn od-btn-secondary" type="button" disabled={busy} onClick={onClose}>Cancelar</button><button className="od-btn od-btn-primary" type="button" disabled={busy||visibility==="selected"&&!selectedUserIds.length} onClick={()=>onSave({visibility,user_ids:visibility==="selected"?selectedUserIds:[],can_contribute:visibility==="private"?false:canContribute})}>Guardar</button></div>{album.is_owner?<section className="od-orangephotos-public-link"><h3>Enlace público</h3><p>Cualquier persona con el enlace podrá ver y descargar las fotos de este álbum.</p>{publicLink.enabled?<><input className="od-filter-input od-orangephotos-public-link__url" readOnly value={fullUrl}/><div className="od-orangephotos-public-link__actions"><button className="od-button-secondary" type="button" disabled={publicBusy} onClick={async()=>{await navigator.clipboard.writeText(fullUrl);setCopyFeedback("Enlace copiado");}}>Copiar enlace</button><button className="od-button-secondary" type="button" disabled={publicBusy} onClick={()=>change(true)}>Regenerar enlace</button><button className="od-button-danger" type="button" disabled={publicBusy} onClick={revoke}>Desactivar enlace</button></div></>:<button className="od-modal-primary" type="button" disabled={publicBusy} onClick={()=>change(false)}>Crear enlace</button>}{copyFeedback?<p className="od-inline-msg">{copyFeedback}</p>:null}{publicError?<p className="od-status-line od-status-line--error">{publicError}</p>:null}</section>:null}</div></section></div>;}
