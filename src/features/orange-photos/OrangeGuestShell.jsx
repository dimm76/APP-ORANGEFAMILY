import { useAuth } from "../../app/authContext.js";
export default function OrangeGuestShell({children}){const{user,logout}=useAuth();const leave=async()=>{await logout();window.history.replaceState({},"","/");window.dispatchEvent(new Event("od-spa-navigate"));};return <div className="od-orangephotos-guest-shell"><header className="od-orangephotos-guest-shell__header"><strong>OrangeFamily</strong><span>{user?.person?.display_name||user?.email}</span><button className="od-button-secondary" type="button" onClick={leave}>Cerrar sesión</button></header><main className="od-orangephotos-guest-shell__main">{children}</main></div>;}
