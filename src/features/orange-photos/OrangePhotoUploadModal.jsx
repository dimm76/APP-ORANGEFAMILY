import {useState} from 'react';
export default function OrangePhotoUploadModal({onClose,onUpload}){const [file,setFile]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e){e.preventDefault();if(!file)return;setBusy(true);setError('');try{await onUpload(file);onClose();}catch(err){setError(err.message);}finally{setBusy(false);}}return <div className="od-modal-backdrop"><section className="od-modal" role="dialog" aria-modal="true"><header className="od-modal-header"><h2 className="od-modal-title">Subir foto o vídeo</h2><button className="od-modal-close" type="button" onClick={onClose}>×</button></header><form className="od-modal-body" onSubmit={submit}><label className="od-orange-photo-upload">Archivo<input type="file" accept="image/jpeg,image/png,image/webp,image/heic,video/mp4,video/quicktime,video/webm" onChange={e=>setFile(e.target.files?.[0]||null)}/></label>{error?<p className="od-status-line od-status-line--error">{error}</p>:null}<div className="od-modal-actions"><button className="od-button-secondary" type="button" onClick={onClose}>Cancelar</button><button className="od-modal-primary" disabled={!file||busy}>{busy?'Subiendo…':'Subir'}</button></div></form></section></div>}
