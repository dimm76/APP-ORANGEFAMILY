function duration(value){if(value==null)return '';const n=Math.round(Number(value));return `${Math.floor(n/60)}:${String(n%60).padStart(2,'0')}`;}
export default function OrangePhotoCard({photo,selected,onSelect,onOpen}){return <article className={`od-orange-photo-card${selected?' is-selected':''}`}><button type="button" className="od-orange-photo-card__media" onClick={()=>onOpen(photo)} aria-label={`Abrir ${photo.title||photo.original_filename}`}><img src={photo.thumbnail_url||photo.preview_url} alt={photo.title||photo.original_filename} loading="lazy"/>{photo.media_type==='video'?<span className="od-orange-photo-card__video">▶ {duration(photo.duration_seconds)}</span>:null}</button><label className="od-orange-photo-card__select"><input type="checkbox" checked={selected} onChange={()=>onSelect(photo.id)}/><span className="od-orange-photo-card__sr">Seleccionar</span></label><div className="od-orange-photo-card__caption"><strong>{photo.title||photo.original_filename}</strong><span>{photo.owner_display_name}</span></div></article>}
