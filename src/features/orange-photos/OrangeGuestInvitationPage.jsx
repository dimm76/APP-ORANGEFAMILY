import { useEffect,useState } from "react"; import { useAuth } from "../../app/authContext.js"; import { acceptOrangeGuestInvitation,getOrangeGuestInvitation } from "./orangeGuestAlbumApi.js";
export default function OrangeGuestInvitationPage({token}){const{user,refreshMe}=useAuth();const[state,setState]=useState({loading:true,error:"",invitation:null});useEffect(()=>{getOrangeGuestInvitation(token).then(r=>setState({loading:false,error:"",invitation:r.invitation})).catch(e=>setState({loading:false,error:e.message,invitation:null}));},[token]);const invitation=state.invitation;const matches=invitation&&String(user?.email||"").trim().toLowerCase()===String(invitation.email||"").trim().toLowerCase();const accept=async()=>{const result=await acceptOrangeGuestInvitation(token);await refreshMe();window.history.replaceState({},"",result.redirect_path);window.dispatchEvent(new Event("od-spa-navigate"));};return <div className="od-page od-orangephotos-guest-invitation"><div className="od-page-inner"><h1 className="od-page-title">Invitación a álbum</h1>{state.loading?<p className="od-status-line">Cargando…</p>:state.error?<p className="od-status-line od-status-line--error">{state.error}</p>:<><h2>{invitation.title}</h2><p>Compartido por {invitation.owner_display_name}</p><p>Invitado: {invitation.email}</p>{!matches?<p className="od-status-line od-status-line--error">Esta invitación está dirigida a otra dirección de correo.</p>:<button className="od-modal-primary" type="button" onClick={accept}>Aceptar invitación</button>}</>}</div></div>;}
