import { useState } from "react";

export default function OrangePhotosBulkEditModal({ mode, count, onClose, onConfirm }) {
  const [value, setValue] = useState(""), [busy, setBusy] = useState(false), dateMode = mode === "date";
  return <div className="od-modal-backdrop"><section className="od-modal" role="dialog" aria-modal="true"><header className="od-modal-header"><h2 className="od-modal-title">{dateMode ? "Cambiar fecha y hora" : "Editar ubicación"}</h2><button className="od-modal-close" type="button" aria-label="Cerrar" onClick={onClose}>×</button></header><div className="od-modal-body"><p>Se aplicará el mismo valor a {count} fotografías.</p><label className="od-form-field"><span className="od-form-label">{dateMode ? "Fecha y hora" : "Ubicación"}</span><input className="od-filter-input" type={dateMode ? "datetime-local" : "text"} value={value} onChange={event => setValue(event.target.value)} /></label><footer className="od-orange-photos-modal__footer"><button type="button" className="od-btn od-btn-secondary" onClick={onClose}>Cancelar</button><button type="button" className="od-btn od-btn-primary" disabled={busy || !value} onClick={async () => { setBusy(true); try { await onConfirm(dateMode ? { captured_at:value } : { location_name:value }); } finally { setBusy(false); } }}>{busy ? "Procesando…" : "Guardar"}</button></footer></div></section></div>;
}
