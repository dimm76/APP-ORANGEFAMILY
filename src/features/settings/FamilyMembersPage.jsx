import { useEffect, useState } from "react";
import { IonBadge } from "@ionic/react";
/* eslint-disable react-hooks/set-state-in-effect */
import { createFamilyMember, listFamilyMembers, updateFamilyMember } from "./familyMembersApi.js";
import "./familyMembers.css";
const MODULES = [["orange_photos", "Orange Photos"], ["wiki", "Wiki"], ["notes", "Notas"], ["documents", "Documentos"], ["finances", "Finanzas"]];
const emptyModuleAccess = Object.fromEntries(MODULES.map(([key]) => [key, false]));
const emptyForm = { first_name: "", last_name: "", email: "", role: "member", module_access: { ...emptyModuleAccess, orange_photos: true } };
const roleLabel = role => role === "owner" ? "Administrador" : role === "guest" ? "Invitado" : "Familiar";
export default function FamilyMembersPage() { const [items, setItems] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [editing, setEditing] = useState(null); const [form, setForm] = useState(emptyForm); const [saving, setSaving] = useState(false); const load = async () => { setLoading(true); try { const result = await listFamilyMembers(); setItems(result.items || []); } catch (loadError) { setError(loadError.message); } finally { setLoading(false); } }; useEffect(() => { void load(); }, []); const openEdit = item => { setEditing(item.person_id); setForm({ first_name: item.first_name, last_name: item.last_name || "", email: item.email || "", role: item.role || "member", has_access: item.has_access, membership_status: item.membership_status, module_access: { ...emptyModuleAccess, ...item.module_access } }); }; const setRole = role => setForm(current => ({ ...current, role, module_access: role === "guest" ? { ...emptyModuleAccess, orange_photos: true } : current.module_access })); const submit = async event => { event.preventDefault(); setSaving(true); try { const payload = { first_name: form.first_name, last_name: form.last_name, email: form.email, role: form.role, module_access: form.module_access }; const result = editing === "new" ? await createFamilyMember(payload) : await updateFamilyMember(editing, { ...payload, has_access: form.has_access, membership_status: form.membership_status }); setMessage(result.invitation_sent === false ? "Usuario guardado, pero el email no pudo enviarse." : "Usuario guardado."); setEditing(null); setForm(emptyForm); await load(); } catch (saveError) { setError(saveError.message); } finally { setSaving(false); } }; return <div className="od-page"><div className="od-page-inner od-page-inner--full od-page-inner--align-stretch"><div className="od-page-header"><div><h1 className="od-page-title">Ajustes · Familiares</h1><p className="od-page-subtitle">Configura los usuarios, roles y accesos de OrangeFamily.</p></div><button type="button" className="od-modal-primary of-family-add-button" onClick={() => { setEditing("new"); setForm(emptyForm); }}>+ Añadir usuario</button></div>{error ? <p className="od-status-line od-status-line--error">{error}</p> : null}{message ? <p className="od-status-line">{message}</p> : null}{loading ? <p className="od-status-line">Cargando familiares…</p> : <div className="od-table-wrap"><table className="od-table od-table--fill"><thead><tr><th>Nombre</th><th>Email</th><th>Rol</th><th>Estado</th><th>Módulos</th><th>Acciones</th></tr></thead><tbody>{items.map(item => <tr key={item.person_id}><td>{[item.first_name, item.last_name].filter(Boolean).join(" ")}</td><td>{item.email || "—"}</td><td><IonBadge className="od-badge od-badge--style-1">{roleLabel(item.role)}</IonBadge></td><td>{item.membership_status === "inactive" ? "Inactivo" : item.auth_status === "pending" ? "Pendiente de activación" : item.auth_status === "active" ? "Activo" : "Sin acceso"}</td><td>{MODULES.filter(([key]) => item.module_access?.[key]).map(([, label]) => label).join(", ") || "Sin módulos"}</td><td>{item.role === "owner" ? "—" : <button type="button" className="od-button-secondary" onClick={() => openEdit(item)}>Editar</button>}</td></tr>)}</tbody></table></div>}{editing ? <div className="od-modal-backdrop"><section className="od-modal"><form onSubmit={submit}><div className="od-modal-header"><h2 className="od-modal-title">Añadir usuario</h2></div><div className="od-modal-body"><label className="od-form-field"><span className="od-form-label">Nombre</span><input className="od-filter-input" required value={form.first_name} onChange={event => setForm({ ...form, first_name: event.target.value })} /></label><label className="od-form-field"><span className="od-form-label">Apellidos</span><input className="od-filter-input" value={form.last_name} onChange={event => setForm({ ...form, last_name: event.target.value })} /></label><label className="od-form-field"><span className="od-form-label">Email</span><input className="od-filter-input" type="email" required value={form.email} onChange={event => setForm({ ...form, email: event.target.value })} /></label><label className="od-form-field"><span className="od-form-label">Rol</span><select className="od-filter-select" value={form.role} onChange={event => setRole(event.target.value)}><option value="member">Familiar</option><option value="guest">Invitado</option></select></label><div className="of-family-modules__grid">{MODULES.map(([key, label]) => <label className="of-family-form__check" key={key}><input type="checkbox" disabled={form.role === "guest"} checked={form.role === "guest" ? key === "orange_photos" : form.module_access[key]} onChange={event => setForm({ ...form, module_access: { ...form.module_access, [key]: event.target.checked } })} />{label}</label>)}</div></div><div className="od-modal-actions"><button type="button" className="od-button-secondary" onClick={() => setEditing(null)}>Cancelar</button><button type="submit" className="od-modal-primary" disabled={saving}>{saving ? "Guardando…" : "Guardar"}</button></div></form></section></div> : null}</div></div>; }
